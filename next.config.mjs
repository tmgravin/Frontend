/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true,
    images: {
      domains: ['msp-academy1.s3.amazonaws.com'], // Add your domain here
    },};

export default nextConfig;

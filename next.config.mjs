/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true,
    images: {
      domains: ['msp-academy1.s3.amazonaws.com','lh3.googleusercontent.com'], // Add your domain here
    },};

export default nextConfig;
